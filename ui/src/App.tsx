import { AppLayout } from "./components/layout/AppLayout";

/** Eva's root: the application shell. All screens live inside AppLayout. */
function App() {
  return <AppLayout />;
}

export default App;
