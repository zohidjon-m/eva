# Eva backend — Phase 0 dependencies.
# Kept deliberately tiny: only what the health spine needs. Later phases add
# their own deps (llama client, chromadb, faster-whisper, kokoro, ...).
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Async HTTP client used to stream tokens from the local llama-server and to
# poll its readiness. (uvicorn[standard] already provides `websockets`, which
# powers both the WS /chat server and scripts/ws_test.py — no extra dep needed.)
httpx==0.28.1
# Test runner — used for the extraction parser unit tests (`python -m pytest`).
# A dev dependency, but pinned here so `pip install -r requirements.txt` gives a
# runnable test suite in one step.
pytest==8.3.4
